module.exports = function check(str, bracketsConfig) {
  let out = false;
  let stack = [];
  if (str % 2 !== 0 || str === '') {
    return out;
  }
  let config = Array.from(bracketsConfig);
  for (let index = 0; index < str.length; index++) {


  }
  function iterating(one_bracket) {
    bracketsConfig.forEach(item => {
      
      item[0] === one_bracket
    })
    let id_bracket = [];

    return
  }
  return out;
}
